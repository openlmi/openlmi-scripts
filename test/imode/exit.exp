#!/usr/bin/expect -f

set timeout 1

expect_after {
    timeout { exit -1 }
}

if {[llength $argv] > 0} {
    set exit_arg " [lindex $argv 0]"
} else {
    set exit_arg ""
}

spawn lmi
expect "lmi> "
send "exit$exit_arg\r"
expect eof
catch wait result
exit [lindex $result 3]
